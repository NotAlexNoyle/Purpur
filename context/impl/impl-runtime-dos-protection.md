---
created: "2026-05-12"
last_edited: "2026-05-12"
---

# Implementation Tracking: runtime-dos-protection

Build site: context/plans/build-site.md

| Task | Status | Notes |
|------|--------|-------|
| T-002 | DONE | Edited `Purpur-Server/src/main/java/net/minecraft/network/chat/contents/TranslatableContents.java`: wrapped `visit(ContentConsumer)` in a try/catch over a new private `TranslatableContentConsumer<T>` static inner class that increments per `accept()` and throws a sentinel `IllegalArgumentException` once visits exceed 32. Outer catch returns `visitor.accept("...")`. Build PASS. Verified against AC R1 a/b/c/d. |
| T-003 | DONE | Edited `Purpur-Server/src/main/java/net/minecraft/network/chat/ComponentUtils.java`: added `updateSeparatorForEntity(...)` and `isValidSelector(Component)` static methods; annotated the existing `Optional<Component>` overload of `updateForEntity` with `@io.papermc.paper.annotation.DoNotUse` (annotation lives in Purpur-API, accessible from server module). Swapped two call sites in `NbtContents.java` (lines 107, 120) and one in `SelectorContents.java` (line 63) from `updateForEntity` to `updateSeparatorForEntity`. Build PASS. Verified against AC R2 a/b/c/d/e. |
| T-004 | DONE-via-existing | No source edit. `Purpur-Server/src/main/java/net/minecraft/network/protocol/game/ServerboundCommandSuggestionPacket.java:17` already reads `buf.readUtf(2048)` — Paper applied this cap earlier than upstream patch 0931. Satisfies AC R3 a/b/c. Test in T-010. |
| T-005 | DONE-via-existing | No source edit. `Purpur-Server/src/main/java/net/minecraft/server/network/ServerGamePacketListenerImpl.java:921-927` already implements the long-no-space spam-kick with the exact upstream predicate (`str.length() > 64 && ((index = str.indexOf(' ')) == -1 || index >= 64)`) and the disconnect uses `Component.translatable("disconnect.spam")` + `PlayerKickEvent.Cause.SPAM` scheduled on main. Satisfies AC R4 a/b/c/d/e/f. Test in T-010. |
| T-007 | DONE | Edited `Purpur-Server/.../com/mojang/brigadier/CommandDispatcher.java`: added `boolean stop = false` before the parse try block; inner `catch (TagParseCommandSyntaxException e)` that sets `stop = true` and rethrows BEFORE the generic `catch (RuntimeException ex)` (catch order matters — typed type must propagate end-to-end); short-circuit `if (stop) return new ParseResults<>(contextSoFar, originalReader, errors);` after `errors.put` and `setCursor`. Build PASS. Verified against AC R6 a/b/c via T-012 DispatcherShortCircuitTest. |
| T-008 | DONE | Tests at `Purpur-Server/src/test/java/io/papermc/paper/chat/TranslatableContentsVisitCapTest.java` covering self-referential arg (terminates with `"..."`), cross-referential chain X→Y→X (terminates), and a short legitimate component (renders without `"..."`). Build + test PASS. AC R1 a/b/c/d. |
| T-009 | DONE | Tests at `.../chat/SeparatorValidationTest.java` covering plain literal valid, NbtContents invalid, SelectorContents invalid, TranslatableContents with NBT arg invalid, with selector arg invalid, with plain arg valid, and nested-recursion-through-translatable-args invalid. Build + test PASS. AC R2 a/b/c/d/e. |
| T-010 | DONE | Tests at `.../network/SuggestionPacketSizeTest.java` (2 tests: 2048 decodes, 2049 throws DecoderException with "longer than" message) and `.../network/SuggestionSpamKickTest.java` (4 tests covering 65 no-space kicks, 200/idx70 kicks, 200/idx5 permitted, 64 no-space permitted — uses local `shouldKickAsSpam` predicate copy mirroring the source at `ServerGamePacketListenerImpl.java:921-927`). Dispatch-ordering criterion (R4 f) verified by source inspection. Build + test PASS. AC R3 a/b/c + R4 a/b/c/d/e/f. |
| T-011 | DONE | Edited `Purpur-Server/.../ServerGamePacketListenerImpl.java` inside the existing `scheduleOnMain(...)` lambda of `handleCustomCommandSuggestions`: after `parseresults = ...getDispatcher().parse(...)` and BEFORE `getCompletionSuggestions(...).thenAccept(...)`, added a check that inspects `parseresults.getExceptions().values()` for `TagParseCommandSyntaxException` and calls `this.disconnect(Component.translatable("disconnect.spam"), PlayerKickEvent.Cause.SPAM)` then `return`. The `return` is what gives R5 c (no suggestion response is sent). Build PASS. AC R5 a/b/c. |
| T-013 | DONE | Part 1 (tests): wrote `.../network/SuggestionHandlerTypedExceptionTest.java` exercising the exception-map inspection predicate against 3 cases (typed in map → true; generic CommandSyntaxException in map → false; empty map → false). Build + test PASS. AC R5 a/b/c. Part 2 (deliverable): committed all source edits in `Purpur-Server` submodule as one "Improve tag parser handling" commit, then ran `./gradlew rebuildPatches` from project root. Emitted `patches/server/0316-Improve-tag-parser-handling.patch` (822 lines) containing hunks for 8 source files + 7 test files. All source-modifying task outputs accounted for. |
