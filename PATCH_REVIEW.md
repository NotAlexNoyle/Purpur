# Custom patch review

Severity: **P0** crash or data loss on a normal server · **P1** breaks a real workflow · **P2** wrong behavior in a reachable case · **P3** minor, cosmetic, or operational.

## 0311 Prevent server from overwriting existing pufferfish.yml

| ID | Sev | Issue | Evidence | Impact | Fix |
|---|---|---|---|---|---|
| 0311-1 | P3 | `newlyCreated` test and the `configFile.exists()` branch are the same check | lines 17-22 | None; readability only | Collapse into if/else |

## 0312 Keep chunks loaded during villager curing

| ID | Sev | Issue | Evidence | Impact | Fix |
|---|---|---|---|---|---|
| CURE-7 | P3 | During a dimension change the new entity briefly holds a ticket at the *source-world* coordinates in the *destination* world. `Entity.restoreFrom` → `load` sets the raw position from the saved `Pos` (`Entity.java:2372`, `setPosRaw`, so the `setPos` override does not fire) then `readAdditionalSaveData` → `startConverting` → `updateConversionTicket` (line 173); only the following `moveTo` (`Entity.java:3516`) re-homes it | lines 123-128, 148-155 | None observable in the normal sequence: Paper defers ticket-level propagation (`ChunkHolderManager.java:354-359`) and `moveTo` removes the misplaced ticket before the next `processTicketUpdates`, so no chunk load is scheduled. Only a ticket-processing call between `restoreFrom` and `moveTo` would load anything | Override `restoreFrom` to release the ticket taken during `load`, and let `setPos` (when converting and no ticket is held) or the first `tick()` acquire it at the final position |

## 0313 Prevent server from writing to config files

| ID | Sev | Issue | Evidence | Impact | Fix |
|---|---|---|---|---|---|
| CFG-1 | P2 | `/whitelist on\|off` is in-memory only. `DedicatedServerSettings.update()` applies and logs instead of `store()`. Chain: `DedicatedPlayerList.setUsingWhiteList` (`DedicatedPlayerList.java:40-42`) → `DedicatedServer.storeUsingWhiteList` (`DedicatedServer.java:837`) → `settings.update` → "Skipping write to immutable server properties file" (`DedicatedServerSettings.java:31,36`) | lines 138-148 | An operator who runs `/whitelist on` during an incident gets an open server after the next restart, with only an INFO line as a hint | Warn the command sender that the change is not persisted, or exempt the whitelist flag from the no-write rule |
| CFG-2 | **P1** | **A fresh install cannot create a world.** `initializeWorldDefaultsConfiguration` builds `paper-world-defaults.yml` with `requireFile=false` and hands it to `trySaveFileNodeIfWritable`, which never writes (`Configurations.java:154-169`). `createWorldConfig` then calls `createDefaultWorldLoader(true, …)` and throws `IllegalStateException("World defaults configuration file '…' doesn't exist")` (`Configurations.java:171-175`, `:204`). `server.properties`, `paper-global.yml`, and per-world `paper-world.yml` are likewise never generated | lines 14-29, 51-55, 138-141 | Not an issue for the live server (files exist). Any new server, test box, or CI run from this build dies at world creation until `config/paper-world-defaults.yml` (at least) is copied in from a mainline run | Write the file when it does not exist (the same rule `0311` uses for `pufferfish.yml`), or document the copy step prominently |
| CFG-3 | P2 | Legacy `paper.yml` is relocated once and its settings are not applied. `PaperConfigurations.setup` moves it to `config/legacy-backup/paper.yml.old`, writes a `paper.yml-README.txt` stub, and runs `convert()` (`PaperConfigurations.java:315-341`); `0313` removed the four writes inside `convert()`, so the converted nodes are built and discarded. The original survives in the backup and the warn on lines 108-109 says to migrate by hand | lines 84, 88, 96, 103, 108-109 | Applies to a first upgrade from a legacy-config server as well as to a `paper.yml` that reappears; settings are not lost, just not carried over | Note it in README |
| CFG-4 | P3 | Title says "config files" but `spigot.yml`, `bukkit.yml`, and `commands.yml` are still rewritten every boot (`SpigotConfig.java:124,130`; `CraftServer.java:362,369,389`) | no hunk for `SpigotConfig` or `CraftServer` | Comment/ordering rewrites and default back-fill still happen for three operator files | Extend the same treatment or narrow the patch title |
| CFG-5 | P3 | Dead code and log noise: `shouldWriteConfigFiles()` is hard-coded false with no override, so the `loader.save(node)` branch is unreachable; every boot and `/paper reload` logs one "Skipping write" line per file | lines 14-29, 140, 146, 163 | Cosmetic | Delete the hook and the dead branch; log at DEBUG or once |

## 0314 Suppress lost connection messages from console

| ID | Sev | Issue | Evidence | Impact | Fix |
|---|---|---|---|---|---|
| LOG-1 | P3 | Passive login-phase disconnects have no console record. `ServerLoginPacketListenerImpl.onDisconnect` is now empty (`ServerLoginPacketListenerImpl.java:206-208`). Server-initiated rejections (whitelist, plugin kicks during login, authentication and proxy-forwarding failures) go through `disconnect(Component)`, which still logs `Disconnecting {}: {}` (`:118`), so those keep a trace; what is lost is the client-side close during login | lines 28-29 | Connection-drop reports during login (bad proxy hop, client crash at login, probing that never completes a handshake) leave no console trace | Keep the login-phase line, or downgrade both to DEBUG instead of deleting |

## 0315 Save player data using a RocksDB database

| ID | Sev | Issue | Evidence | Impact | Fix |
|---|---|---|---|---|---|
| DB-2 | P0 | Conflicts with MyWorlds + BKCommonLib on the live server (`beta-2-plugins/MyWorlds/config.yml:15` `useWorldInventories: true`, main inventory group `world`; `PlayerFileDataHandler` classes in `BKCommonLib-1.19.4-v2.jar` hook `PlayerDataStorage.load/save/getSeenPlayers` and MyWorlds reads and writes `<world>/playerdata/<uuid>.dat` directly). The constructor runs `migrate()` before any plugin loads and renames that directory | line 260 (`Files.move` to `playerdatamigrated`) | First boot on live: every player's main-group inventory, ender chest, XP, and position appear empty. Recovery: stop, roll back to a build without this patch, delete the new `playerdata`, rename `playerdatamigrated` back, delete `playerdatadb`. Deleting `playerdatadb` alone is not enough: with this patch still installed the marker is gone, so the next boot migrates and renames again. Anything written to the new files since the upgrade is lost either way | Do not rename the source directory; make migration a no-op while the folder is in active use; decide whether RocksDB storage is meaningful at all with MyWorlds owning player files |
| DB-3 | P1 | With MyWorlds hooked, RocksDB is a stale snapshot. Online-player saves go through `PlayerList.playerIo` (`PlayerList.java:154,561`), which BKCommonLib replaces, and never reach `saveCompoundTag`; `CraftOfflinePlayer` and `CraftServer.getOfflinePlayers` read the unhooked `console.playerDataStorage` (`CraftOfflinePlayer.java:41`, `CraftServer.java:2127`) | lines 297-299 (`save`), 383-394 (`getPlayerData`), 454-456 and 540-552 (`CraftOfflinePlayer`) | `hasPlayedBefore()` false for everyone who first joined after the upgrade; `getLastPlayed/getLastSeen` frozen at migration time for offline players (online players still answer from the live entity); Purpur's offline `setLocation`/`teleportOffline` write a record nothing reads | There is no routing fix inside the server: BKCommonLib's hook overrides `load/save/getSeenPlayers` only, so pointing `CraftOfflinePlayer` at `playerIo` would still call the unhooked `getPlayerData`. Mainline works because `CraftOfflinePlayer` and MyWorlds read the same `.dat` file. Either keep `.dat` files as the source of truth for offline reads, or drop the patch |
| DB-4 | P1 | Migration is all-or-nothing and aborts server start. Every `*.dat` regular file goes through `UUID.fromString` and `readCompressed`; any `IllegalArgumentException` or `IOException` sets `success=false` and throws. Vanilla's own save path leaves `<uuid>-<random>.dat` temp files behind on a crash mid-save, and they match the `endsWith(".dat")` filter | lines 239-258 | One leftover temp file or one corrupt file → "Failed to start the minecraft server" on every boot until an operator deletes it. Vanilla only failed for that one player. "All-or-nothing" is about startup, not the data: records migrated before the bad file are already in the DB when the constructor throws, and the marker is not, so the next boot re-migrates them | Skip and log unparsable files; write the marker only after the survivors are migrated |
| DB-5 | P2 | No `.dat_old` backups and no export back to `.dat`. `load()` catches any `Exception` from `get()` (including a transient `RocksDBException`) and continues with a null tag, so the player loads fresh and the next save overwrites the record. The catch-and-continue is inherited from vanilla and logs a WARN; what is new is that the overwritten previous value no longer exists anywhere | line 277 (bare `put`), 353-355 (`load` swallow) | Downgrade to mainline cannot see any change since migration (the DB itself is untouched); a transient DB read error followed by one save replaces the record with a fresh player, with one WARN line as the only trace | Keep a previous-value copy per key, or an export command |
| DB-6 | P3 | Not applicable to TrueOG (backups are taken with the server stopped, and `close()` runs `syncWal()` first); recorded for anyone copying a running server. `playerdatadb` cannot be hot-copied safely: writes sit in the WAL until a memtable flush, nothing flushes on `/save-all` or autosave, so `rsync`/`tar` of a live server can capture a torn `MANIFEST`/WAL | lines 130-137 (no flush policy), 171-188 (`syncWal` only at close) | None here | `flush()` + RocksDB `Checkpoint` from `MinecraftServer.saveEverything` if live backups are ever introduced |
| DB-8 | P3 | Marker written before the rename. If `playerdatamigrated` already exists (operator restored `playerdata` and deleted the DB), `Files.move` throws after the marker persisted, so the next boot skips migration | lines 259-260 | The records were copied before the marker, so nothing is missing on the next boot; the server fails to start once (`Could not successfully migrate player data`), then starts with the directory still in place and silently ignores it from then on, so any later edits or restores into `playerdata` never reach the DB | Rename first, then write the marker; or use a timestamped target |
| DB-9 | P3 | `saveCompoundTag`/`saveCompoundTagCustom` after `close()` return silently, no log. Shutdown order: `disablePlugins` (`MinecraftServer.java:960`; waits up to 5 s for running Bukkit async workers and logs each survivor at SEVERE, `CraftServer.java:1061-1075`) → `playerList.saveAll` (`:971`) → `saveAllChunks` (`:988`) → `Util.shutdownExecutors` (`:1011`) → `playerDataStorage.close()` (`:1013`) | lines 272, 285 | An async task that outlives the 5 s grace and calls `CraftOfflinePlayer.save` after `close()` has its save dropped with no log line for the drop itself | `LOGGER.warn` on the dropped save |
| DB-10 | P3 | `close()` sets a volatile flag then frees native handles; a `get`/`put` that passed the flag check on another thread dereferences a freed handle. Off-main callers exist (`AsyncPlayerPreLoginEvent` handlers calling `hasPlayedBefore()`, async scheduler tasks). BKCommonLib's hook copies fields into a second instance whose `closed` flag is never set | lines 171-188, 272-277, 301-304 | SIGSEGV / `hs_err` at shutdown in the narrow window after the network stops; the 5 s worker wait in `disablePlugins` narrows it but does not close it. Kept at P3 because a live occurrence has not been shown | Read-write lock around native calls, or `close()` after all plugin threads are joined |
| DB-12 | P3 | `CraftOfflinePlayer` first/last-played/login/seen return 0 instead of the file's mtime when the NBT key is absent; the `load()` block that seeded `firstPlayed` from mtime is also gone. Affects any record without the keys: vanilla imports, records written by plugins through NMS, and anything old enough to predate them | lines 357-367, 468-536 | Inactivity-purge plugins that treat `getLastPlayed()==0` with `hasPlayedBefore()==true` as "never seen" purge those players. Kept at P3 because no such record is known on the live server | Return `-1` or the migration timestamp instead of 0 |
| DB-13 | **P2** | **`getPlayerDataCustom` after `close()` segfaults the JVM.** `close()` clears `worldHandles` (line 178) before `closeE()`, so after shutdown every world name misses the map and `getOrCreateWorldColumnFamily` calls `rocksDB.createColumnFamily` on a closed database (line 195). `getPlayerDataCustom` has no `closed` check before that call (lines 402-406); `saveCompoundTagCustom` checks `closed` (line 285) but not atomically with the create. Verified: `createColumnFamily` on a closed `RocksDB` is a `SIGSEGV`, not an exception (rocksdbjni 10.10.1.1) | lines 171-206, 285-290, 402-406 | Any plugin using the new per-world API from an async task that is still running when `stopServer` reaches line 1013 kills the process with an `hs_err` instead of a clean exit. Deterministic, unlike DB-10. No current caller is known | Check `closed` inside `getOrCreateWorldColumnFamily` and return `null` (the resulting `NullPointerException` is already caught; verified `put(null, …)`/`get(null, …)` throw NPE). Not clearing `worldHandles` in `close()` is not enough on its own, since an unseen world name still reaches `createColumnFamily`. The DB-10 lock would cover both |
| DB-14 | P3 | Reads create column families. `getPlayerDataCustom` goes through `getOrCreateWorldColumnFamily` (line 404), so a lookup for a world name that has no data creates a persistent `world:<name>` column family on disk. Nothing ever drops one, so typo'd, renamed, or deleted world names accumulate in `playerdatadb` forever | lines 190-206, 402-415 | Operational clutter; each column family carries its own memtable and SST files | Look up without creating on the read path; add a way to drop a world's column family |
| DB-15 | **P2** | Player serialization moved outside the save `try`. Vanilla's `save(Player)` called `player.saveWithoutId(...)` inside the `try`/`catch (Exception)` that logs "Failed to save player data" (pre-patch `PlayerDataStorage.java:34-45` via `git show 04830850d`). The new `save` evaluates `player.saveWithoutId(new CompoundTag())` as an argument to `saveCompoundTag` (line 298), before that method's `try` (line 274) is entered, so any exception thrown while serializing the player (a plugin-created vehicle or passenger whose NBT write throws → `ReportedException` out of `Entity.saveWithoutId`) propagates out of `PlayerList.save` into the caller | lines 297-299 | Autosave (`MinecraftServer.java:1441` → `playerList.saveAll`) or a quit during that state crashes the tick loop instead of logging one failed save. Exceptional-state regression, not a demonstrated live-server case | Move the `saveWithoutId` call inside the `try` in `save(Player)`, matching vanilla |
